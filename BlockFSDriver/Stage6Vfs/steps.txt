Assignment #06

a. Complete the TODOs in the file real_dfs_bb.c in here.
b. Compile the real_dfs_bb.c driver by typing "make".
c. Load the driver dfs_bb.ko using "insmod dfs_bb.ko".
d. Execute the following:
	> grep sfs /proc/filesystems
	> sudo mount -t sfs /dev/rb1 /mnt
	> sudo mount -t sfs /dev/rb3 /mnt # No error even if no DFS file system on /dev/rb3. Why?
	> mount | grep dfs
	> sudo umount /mnt
	> df # Observe the error. Why?
	> stat /mnt
	> cd /mnt # Observe the error. Why?
	> ls /mnt # Observe the error. Why?
	> sudo umount /mnt
e. Unload the driver sfs_bb.ko using "rmmod dfs_bb".
